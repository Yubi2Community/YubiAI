#!/usr/bin/env bash

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"
cd $SCRIPT_DIR

FILENAME="count_1w.txt"
if [[ -f $FILENAME ]]; then
  echo "Dataset already downloaded"
else
  echo "Downloading the dataset..."
  wget https://norvig.com/ngrams/${FILENAME}
fi